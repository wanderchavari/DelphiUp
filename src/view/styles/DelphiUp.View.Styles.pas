unit
  DelphiUp.View.Styles;

interface

uses
  System.UITypes;

const
  {--- Cores Padrao Bootstrap ---}
  {
  PRIMARY     = $FF007bff;
  SECONDARY   = $FF6c757d;
  SUCCESS     = $FF28a745;
  DANGER      = $FFdc3545;
  WARNING     = $FFffc107;
  INFO        = $FF17a2b8;
  LIGHT       = $FFf8f9fa;
  DARK        = $FF343a40;
  WHITE       = $FFffffff;
  BLACK       = $FF000000;
  }
  {--- Cores Padrao MarketUp ---}
  //PRIMARY     = $FF007bff;
  PRIMARY     = $FF8bc53d;
  SECONDARY   = $FF6c757d;
  SUCCESS     = $FF28a745;
  DANGER      = $FFdc3545;
  WARNING     = $FFffc107;
  INFO        = $FF658c33;
  LIGHT       = $FFa2d164;
  DARK        = $FF343a40;
  WHITE       = $FFffffff;
  BLACK       = $FF000000;

var
  BACKGROUND : TAlphaColor = WHITE;
  FONT_COLOR : TAlphaColor = SECONDARY;
  FONT_COLOR_CONTRAST : TAlphaColor = WHITE;

implementation

end.
